server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Support being served under /radar/ via edge handle_path
    location / {
        try_files $uri $uri/ /index.html;
    }
}
